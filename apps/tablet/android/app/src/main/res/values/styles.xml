<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Ce qui dépasse de la WebView (zone de la caméra, barres) prend le bleu AfriKaisse, pas du blanc. -->
        <item name="android:windowBackground">@color/ic_launcher_background</item>
        <!-- Barres système dans le bleu foncé de la barre d'application AfriKaisse. -->
        <item name="android:statusBarColor">#173F6B</item>
        <item name="android:navigationBarColor">#173F6B</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Android 12+ : fond de l'écran de lancement système, derrière l'icône. -->
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
    </style>
</resources>