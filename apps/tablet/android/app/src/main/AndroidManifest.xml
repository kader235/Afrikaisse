<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <!-- HTTP en clair autorisé pour UNE raison : le serveur local du restaurant répond
             en HTTP sur le Wi-Fi (192.168.x.x), sans certificat. Android ne permet pas de
             règle par plage d'adresses : c'est l'application qui refuse HTTP vers toute
             adresse hors réseau local (normalizeServerUrl, packages/core/src/network.ts).
             Le Cloud AfriKaisse n'est joint qu'en HTTPS. -->

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Notifications push (Firebase) : canal et icône par défaut quand l'application est fermée.
             Le canal « afk-service » (importance haute, son, vibration) est créé par l'application. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="afk-service" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_afrikaisse" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Impression thermique. Bluetooth : appairage classique (SPP) des imprimantes de tickets.
         Avant Android 12, BLUETOOTH/BLUETOOTH_ADMIN suffisent (normales) ; à partir d'Android 12,
         BLUETOOTH_CONNECT est demandée à l'exécution. USB : imprimante branchée en OTG. Wi-Fi/TCP
         passe par INTERNET (déjà là). Aucune n'est requise : une tablette sans imprimante fonctionne. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
</manifest>
