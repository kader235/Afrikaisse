{
  "name": "@afrikaisse/tablet",
  "version": "0.1.0",
  "private": true,
  "description": "Application Android pour tablette : les écrans de apps/web embarqués dans une enveloppe Capacitor",
  "type": "module",
  "scripts": {
    "sync": "npm run build -w @afrikaisse/web && cap sync android",
    "apk": "node scripts/apk.mjs"
  },
  "dependencies": {
    "@aparajita/capacitor-secure-storage": "^8.0.0",
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
